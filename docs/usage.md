# Usage

1. Open the desktop app.
2. Select a preset such as `JWT Secret`.
3. Choose an encoding and output format.
4. Click Generate.
5. Review entropy and security level.
6. Copy manually when ready.

For `.env` output, use placeholders in documentation and real generated values
only in local or deployment secret storage.
